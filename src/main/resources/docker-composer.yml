version: "3.9"
services:
  postgres:
    image: postgres:latest
    container_name: playtox_test
    environment:
      POSTGRES_DB: "mydatabase"
      POSTGRES_USER: "user"
      POSTGRES_PASSWORD: "root"
    ports:
      - "5432:5432"